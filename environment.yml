name: sam3test
channels:
  - pytorch
  - nvidia
  - conda-forge

dependencies:
  - python=3.12

  - pip

  - pip:
      - torch==2.12.0
      - torchvision
      - --extra-index-url https://download.pytorch.org/whl/cu128
      - git+https://github.com/facebookresearch/sam3.git